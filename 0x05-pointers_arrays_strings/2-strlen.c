#include <string.h>
#include "main.h"

/**
 * _strlen - takes a pointer of a string and return its length
 * @s: paraeter to check
 * Return: 0 (Success)
 */

int _strlen(char *s)
{
	return (strlen(s));
}
